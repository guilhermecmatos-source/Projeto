PORT=4000
NODE_ENV=development

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=fleet_ai

# JWT
JWT_SECRET=fleet-ai-super-secret-change-in-production
JWT_EXPIRES_IN=7d

# Demo mode (usa dados mock quando DB indisponível)
USE_MOCK_DATA=true
