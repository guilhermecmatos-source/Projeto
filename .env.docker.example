# Copie para .env na raiz: cp .env.docker.example .env
MYSQL_ROOT_PASSWORD=fleetai_root
MYSQL_PASSWORD=fleetai123
JWT_SECRET=fleet-ai-docker-secret-change-me

# true = demo sem MySQL | false = usa banco (aguarde mysql healthy)
USE_MOCK_DATA=true

# CORS: * libera acesso de qualquer IP (celular na rede)
CORS_ORIGIN=*
